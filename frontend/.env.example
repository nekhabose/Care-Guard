# Base URL of the CareGuard FastAPI backend.
# Leave blank to use the Vite dev proxy (/dashboard -> http://localhost:8000).
VITE_API_BASE_URL=

# Optional: override the dev proxy target (defaults to http://localhost:8000)
VITE_API_PROXY_TARGET=http://localhost:8000
